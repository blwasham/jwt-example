command to run api

$ npm start
- starts on localhost:3000
- package json calls nodemon server.js
- have to call through start b/c I installed nodemon locally, not globally

command to run app
$ http-server
- starts on localhost:8080

Things installed globablly
- npm-run
- http-server
